export enum ButtonSeverity {
  Primary = 'primary',
  Secondary = 'secondary',
  Success = 'success'
}

export enum ButtonSize {
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
  Wide = 'wide'
}

export enum ButtonStyle {
  Default = 'default',
  Icon = 'icon'
}

export enum ButtonType {
  Button = 'button',
  Reset = 'reset',
  Submit = 'submit'
}
